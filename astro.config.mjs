import { defineConfig } from 'astro/config';

export default defineConfig({
  site: 'https://christinambradley.com',
  // If deploying to GitHub Pages WITHOUT a custom domain, uncomment the line below
  // and replace "your-repo-name" with your actual GitHub repository name:
  // base: '/your-repo-name',
});
